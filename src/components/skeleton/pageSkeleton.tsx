import { Skeleton } from "@/components/ui/skeleton"

const PageSkeleton = () => {
  return (
    <Skeleton className="flex flex-col min-w-full min-h-screen bg-[#1C1C1F] rounded-none"/>
  )
}

export default PageSkeleton